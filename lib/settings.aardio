import loadini;
import sys;
namespace settings;

userSettings = ..loadini.loadConfigIni()

webServer = {
	"addr": "127.0.0.1",
	"port": 60025,
	"user": "root",
	"password": "toor"
}

webApi = ..string.format("http://%s:%d", webServer.addr, webServer.port);

serviceName = "FrpcManager";

logFile = "\frpcmanager.log"
